<resources>
    <bool name="config_largeHeap">true</bool>
    <bool name="is_tablet">false</bool>
    <bool name="is_large_tablet">false</bool>


    <!-- Max number of page indicators to show -->
    <integer name="config_maxNumberOfPageIndicatorsToShow">21</integer>

<!-- DragController -->
    <integer name="config_flingToDeleteMinVelocity">-1500</integer>

<!-- AllApps/Customize/AppsCustomize -->
    <!-- The alpha of the AppsCustomize bg in spring loaded mode -->
    <integer name="config_appsCustomizeSpringLoadedBgAlpha">65</integer>
    <integer name="config_workspaceUnshrinkTime">0</integer>
    <integer name="config_overviewTransitionTime">0</integer>

    <!-- Out of 100, the percent to shrink the workspace during spring loaded mode. -->
    <integer name="config_workspaceSpringLoadShrinkPercentage">70</integer>
    <!-- Out of 100, the percent to shrink the workspace during overview mode. -->
    <integer name="config_workspaceOverviewShrinkPercentage">65</integer>

    <!-- Fade/zoom in/out duration & scale in the AllApps transition.
         Note: This should be less than the workspaceShrinkTime as they happen together. -->
    <integer name="config_appsCustomizeZoomInTime">0</integer>
    <integer name="config_appsCustomizeZoomOutTime">0</integer>
    <integer name="config_appsCustomizeZoomScaleFactor">0</integer>
    <integer name="config_appsCustomizeFadeInTime">0</integer>
    <integer name="config_appsCustomizeFadeOutTime">0</integer>
    <integer name="config_appsCustomizeWorkspaceShrinkTime">0</integer>
    <integer name="config_appsCustomizeWorkspaceAnimationStagger">0</integer>
    <integer name="config_workspaceAppsCustomizeAnimationStagger">0</integer>

    <integer name="config_workspaceDefaultScreen">0</integer>

    <!-- Tab transition animation duration -->
    <integer name="config_tabTransitionDuration">0</integer>

    <!-- The slope, in percent, of the drag movement needed to drag an item out of
         AppsCustomize (y / x * 100%)  -->
    <integer name="config_appsCustomizeDragSlopeThreshold">150</integer>

<!-- Workspace -->
    <!-- Whether or not the drop targets drop down as opposed to fade in -->
    <bool name="config_useDropTargetDownTransition">true</bool>
    <!-- Whether or not to fade the side pages -->
    <bool name="config_workspaceFadeAdjacentScreens">true</bool>

    <!-- The transition duration for the background of the drop targets -->
    <integer name="config_dropTargetBgTransitionDuration">0</integer>

    <!-- The duration (in ms) of the fade animation on the object outlines, used when
         we are dragging objects around on the home screen. -->
    <integer name="config_dragOutlineFadeTime">0</integer>

    <!-- The alpha value at which to show the most recent drop visualization outline. -->
    <integer name="config_dragOutlineMaxAlpha">128</integer>

    <!-- Parameters controlling the animation for when an item is dropped on the home screen,
         and it animates from its old position to the new one. -->
    <integer name="config_dropAnimMinDuration">250</integer>
    <integer name="config_dropAnimMaxDuration">300</integer>

    <!-- The duration of the UserFolder opening and closing animation -->
    <integer name="config_folderAnimDuration">0</integer>

    <!-- The distance at which the animation should take the max duration -->
    <integer name="config_dropAnimMaxDist">0</integer>

    <!-- Properties controlling the workspace fade-out during dragging -->
    <integer name="config_dragFadeOutAlpha">80</integer>
    <integer name="config_dragFadeOutDuration">0</integer>

    <!-- Camera distance for the overscroll effect -->
    <integer name="config_cameraDistance">8000</integer>

    <!-- Whether or not to use custom clings if a custom workspace layout is passed in -->
    <bool name="config_useCustomClings">false</bool>

<!-- Hotseat -->
    <bool name="hotseat_transpose_layout_with_orientation">true</bool>

    <!-- Memory debugging, including a memory dump icon -->
    <bool name="debug_memory_enabled">false</bool>

    <!-- Name of a subclass of com.fraggel.launcher.AppFilter used to
         filter the activities shown in the launcher. Can be empty. -->
    <string name="app_filter_class" translatable="false"></string>

    <bool name="show_search_bar">true</bool>
    <bool name="allow_rotation">false</bool>
    <bool name="show_customcontent">false</bool>
    <string name="app_custom_contentPackage">com.google.android.googlequicksearchbox</string>
    <integer name="workspace_icons">7</integer>
    <integer name="hotseat_icons">7</integer>
    <integer name="allapps_icons">5</integer>
    <bool name="show_google_bar">false</bool>
    <integer name="widgets_rows">3</integer>
    <integer name="widgets_cols">2</integer>
    <integer name="allapps_rows">6</integer>
    <integer name="allapps_cols">4</integer>
    <integer name="allapps_transparency">10</integer>
    <integer name="workspace_rows">4</integer>
    <integer name="workspace_cols">4</integer>

</resources>
